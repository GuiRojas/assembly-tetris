getMatriz:
    ;+1 --> anda 1 byte
    mov matrix + 15, 10
    mov matrix + 16, 85
    mov matrix + 18, 100    

    xor eax,eax
    mov al, byte ptr [matrix]
    mov al, byte ptr [matrix+15]
    mov al, byte ptr [matrix+4*4+2]    
    
    invoke  dwtoa, eax, offset randomnum ;double word to ascii
    invoke  StdOut, offset randomnum      ;printa em console o valor
